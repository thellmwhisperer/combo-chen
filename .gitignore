# lavish artifacts are working surfaces, not repo content
.lavish/

# the user's global gitignore excludes AGENTS.md; this repo ships its charter
!AGENTS.md

node_modules/
dist/
*.log
.DS_Store
output/
# local config (the example ships, the real one doesn't)
combo-chen.toml
.no-mistakes.yaml
